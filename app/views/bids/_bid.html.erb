<h1>Listing Offers</h1>
 <br />
<%#= @product.bids %>
<%#= @product.bids.all %>
<% @product.bids.reverse.each do |bid| %>
<%#= bid %>
<div class="comments cmnt-name">
<div class="col-sm-1 cmnt-name">
    <%= image_tag(bid.user.avatar.url(:small),{class:"img-circle img-responsive"}) %>
      <%# if !bid.user.avatar.blank? %>
        <%#= link_to image_tag(comment.user.avatar.url(:thumb),{class:"img-circle img-responsive"}),profile_path(:user_id => bid.user_id) %>
      <%# else %>
        <%#= link_to image_tag("user.jpg",{class:"img-circle img-responsive"}),profile_path(:user_id =>bid.user_id) %>
      <%# end %>
 </div>
 <% if !bid.content.blank? %>
   
   <p class="pull-right"><%= bid.created_at.strftime("%Y/%m/%d") %></p>
<% end %>
<p><%= link_to bid.user.fullname,profile_path(:user_id => bid.user_id),{class:"commentor"} %>
    
&nbsp;<br><span style="color:blue">Price: </span><%= bid.price %>,&nbsp;<span style="color:blue">Message:</span><%= bid.content %></p>
   <%# if !bid.content.blank? %>
   <p><%#= link_to "Remove", [comment.product,comment],{ method: :delete, data: { confirm: "Are you sure?" }} %></p> 
   <p><%= link_to "Remove", [bid.product,bid],{ method: :delete, data: { confirm: "Are you sure?" }} %></p> 
   <%# end %>
<div>
<% end %>