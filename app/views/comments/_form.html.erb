  <% if logged_in? %>
   <%= form_for([@product, Comment.new]) do |f| %>
   <div class="col-sm-11">
      <%= f.text_field :message, :placeholder => 'Write Comment...' , :class => "form-control" %>
      </div>
      <span class="input-group-btn">
         <%= button_tag "", type: 'submit', class: "btn btn-default glyphicon glyphicon-comment pull-left" %>%>
      </span>
   <% end %>
 <% end %>
